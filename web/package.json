{
  "name": "agentnudge-widget-build",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "build": "esbuild src/widget.js --bundle --format=iife --target=es2020 --minify --legal-comments=inline --outfile=dist/widget.js"
  },
  "dependencies": {
    "html2canvas": "^1.4.1"
  },
  "devDependencies": {
    "esbuild": "^0.25.0"
  }
}
