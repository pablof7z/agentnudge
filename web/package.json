{
  "name": "agentnudge-widget-build",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "build": "esbuild src/widget.js --bundle --format=iife --target=es2020 --loader:.svg=text --minify --legal-comments=inline --outfile=dist/widget.js",
    "test": "node --test test/*.test.js"
  },
  "dependencies": {
    "@tabler/icons": "^3.46.0",
    "dompurify": "^3.4.13",
    "html2canvas": "^1.4.1",
    "marked": "^18.0.9"
  },
  "devDependencies": {
    "esbuild": "^0.25.0",
    "jsdom": "^27.4.0"
  }
}
